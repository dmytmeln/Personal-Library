spring:
  config:
    activate:
      on-profile: dev

  jpa:
    properties:
      hibernate:
        generate_statistics: true
        show_sql: true
        format_sql: true

logging:
  level:
    org.example.library: DEBUG